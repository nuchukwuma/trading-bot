{
  "name": "smc-alert-bot",
  "version": "1.0.0",
  "description": "Smart Money Concepts trading alert bot for Deriv synthetics and forex \u2014 analysis and alerts only, no auto-execution.",
  "main": "src/index.js",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "scan": "node src/index.js --once",
    "test": "node --test \"tests/**/*.test.js\"",
    "test:watch": "node --test --watch \"tests/**/*.test.js\"",
    "calibrate": "node scripts/calibrate.js",
    "backtest": "node scripts/backtest.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "mongoose": "^8.5.1",
    "ws": "^8.18.0"
  },
  "license": "MIT",
  "private": true
}
